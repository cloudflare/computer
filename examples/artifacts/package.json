{
  "name": "@example/computer-artifacts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Example Worker that turns examples/worker-shell into a cloneable Cloudflare Artifacts repo through @cloudflare/computer.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build:types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/computer": "*"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wrangler": "^4.130.0"
  }
}
