{
  "name": "@example/workspace-tutorial",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tutorial Worker where an AIChatAgent looks up a recipe, renders a markdown card, and converts it to a PDF with pandoc in a @cloudflare/workspace container.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types --env-file=.env.example"
  },
  "dependencies": {
    "@cloudflare/ai-chat": "^0.10.1",
    "@cloudflare/workspace": "*",
    "agents": "^0.20.1",
    "ai": "^6.0.196",
    "workers-ai-provider": "^3.1.14",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260616.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.96.0"
  }
}
